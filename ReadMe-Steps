Sonar Integration
------------------
Create a Maven project
Create a Main class with some function
Download sonarqube zip file from https://www.sonarqube.org/success-download-community-edition/
Go to conf/sonar.properties and uncomment webport 9000
Go to bin folder/<YourOS> and run sonar using ./sonar.sh start (other options: console | start | stop | force-stop | restart | status | dump)
Check sonarqube page by typing localhost:9000 in browser
Configure SonarLint in eclipse by searching for sonarlint in eclipse marketplace
Restart Eclipse for sonarlint to get added to project properties. check if automatic check is enabled
In project properties, go to SonarLint and bind to sonarqube
provide server address as http://localhost:9000, userID and Password (default is admin)
Generate a token and copy the java maven command
Run Maven configuration with following command
sonar:sonar \
  -Dsonar.projectKey=CodeScan \
  -Dsonar.host.url=http://localhost:9000 \
  -Dsonar.login=<token generated by sonar>
or command line
  mvn sonar:sonar \
  -Dsonar.projectKey=CodeScan \
  -Dsonar.host.url=http://localhost:9000 \
  -Dsonar.login=<token generated by sonar>
  
  
  GitHub
  create a repository after login to GIT
  https://github.com/ansuman1/Integrations.git
  
  git clone https://github.com/ansuman1/Integrations.git
  git checkout -b onlySonar
  git add <xx.*>
  git status
  git commit -m "description"
  git push origin onlySonar 
  